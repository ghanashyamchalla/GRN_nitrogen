GRN nitrogen model driver:

----------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------
Linux:
----------------------------------------------------------------------------------------------------------
Method 1: Directly calling using the executable

cd to the GrNM/ directory
Run:
Execute the program: matlab -nodisplay -nosplash -r "addpath(genpath('./')); GrNM('GrNM_input.txt', 'GrNM_output'); exit;"
----------------------------------------------------------------------------------------------------------
Method 2: Using a bash script
cd to the GrNM/ directory
Create executable: chmod +x GrNM.sh
Run script: ./GrNM.sh

----------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------
Windows:
To be added later
